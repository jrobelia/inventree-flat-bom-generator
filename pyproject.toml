#This file is used to package the FlatBOMGenerator plugin.
#
#- It was generated by the InvenTree Plugin Creator tool - version 1.13.3
#- Ref: https://github.com/inventree/plugin_creator

[build-system]
requires = ["setuptools", "twine", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "inventree-flat-bom-generator"
description = "Displays flattened bill of materials for assemblies with recursive traversal and export capabilities"
dynamic = ["version"]
authors = [
    { name = "Jon Robelia",email = "jonr@openaeros.com" },
]
readme = "README.md"
license = "MIT"
keywords = ["inventree", "plugin"]
requires-python = ">=3.9"
dependencies = [
    # Enter your plugin library dependencies here
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Framework :: InvenTree",
]

[project.entry-points."inventree_plugins"]
FlatBOMGenerator = "flat_bom_generator.core:FlatBOMGenerator"

[tool.setuptools.packages.find]
where = [""]
exclude = ["*.tests", "*.tests.*", "tests.*", "tests"]

[tool.setuptools.dynamic]
version = {attr = "flat_bom_generator.PLUGIN_VERSION"} 

[tool.ruff]
exclude = [
    ".git",
    "__pycache__",
    "test.py",
    "tests",
    "venv",
    "env",
    ".venv",
    ".env",
]

src=["flat_bom_generator"]

[tool.ruff.lint.per-file-ignores]
# The repo-root __init__.py (required for plugin-dir discovery in the
# devcontainer) makes the checkout directory part of the package chain.
# In a standalone checkout the outermost dir is the hyphenated repo name,
# which trips N999 on every __init__.py below it. N999 on __init__.py only
# ever checks the containing directory name, so this suppression is narrow.
"**/__init__.py" = ["N999"]
